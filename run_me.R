# Run these commands only once to install packages:
install.packages("tidySEM", dependencies = TRUE)
install.packages("foreign", dependencies = TRUE)
install.packages("readxl", dependencies = TRUE)
install.packages("semTools", dependencies = TRUE)

# Run these commands every time to load the installed packages:
library(tidySEM)
library(foreign)
library(readxl)
library(semTools)

# Then, write your analysis code in a script below